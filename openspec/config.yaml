# ────────────────────────────────────────────────────────────
# OpenSpec — crono-job
# Auto-generated by sdd-init. Do not edit manually.
# ────────────────────────────────────────────────────────────

project: crono-job
version: 1.0.0
stack:
  runtime: node
  build: vite 6
  framework: react 19
  language: typescript 5.7 (strict)
  css: tailwindcss v4
  key_libs:
    - jspdf 2.5
    - html-to-image 1.11
    - vite-plugin-pwa 0.21
  deploy: vercel

context: >
  Monthly work calendar PWA ("cronograma de trabajo mensual").
  Users select rest days in a month grid and export a themed PDF
  (dark/light, portrait/landscape). Single-user, client-only SPA.
  Spanish UI (es-AR) including PWA manifest. No API, no database,
  no state persistence beyond in-memory React state.

conventions:
  ui_lang: es-AR
  components_in_src: true
  pure_helpers_separated: true     # exportPdf.ts is a pure module
  no_test_infrastructure: true
  no_linter: true
  no_formatter: true
  typescript_strict: true

strict_tdd: false
strict_tdd_reason: >
  No test runner or test dependencies found in package.json.
  No test or spec files exist. Strict TDD requires a test runner
  before it can be enforced.

testing:
  runner:
    available: false
    reason: No test dependencies in package.json (no vitest, jest, etc.)
  layers:
    unit: false
    integration: false
    e2e: false
  coverage:
    available: false
  quality:
    linter: false
    type_checker: true
    formatter: false

phases:
  proposal: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify: {}
  archive: {}

persistence:
  mode: openspec
  created_at: 2026-06-25
